# Tutorials

Start with the raw-to-denoised walkthrough. The external-whitelist tutorial is for libraries whose Cell Ranger filtered barcodes are missing or unreliable. {doc}`from_r` runs the same Python workflow from R (`reticulate` or the CLI).

```{toctree}
:maxdepth: 2

raw_to_denoised
from_r
external_cell_whitelists
reports_and_plots
```
